keycloak:
  server_url: "http://keycloak:8080/"
  realm_name: "fairdi_nomad_test"

logstash:
  enabled: false

temporal:
  enabled: true
client:
  url: "http://localhost:8000/nomad-oasis/api"
  user: "test"
  password: "password"
services:
  upload_limit: 1000

plugins:
  entry_points:
    options:
      nomad_north_jupyter.north_tools:jupyter:
        id_url_safe: north-jupyter
